---
import CartWidget from '@components/react/CartWidget.tsx'
import CartNotification from '@components/react/CartNotification.tsx'
import CartDebugButton from '@components/react/CartDebugButton.tsx'

export interface Props {
  title?: string
  description?: string
}

const { 
  title = "Tienda Online - Productos de Calidad",
  description = "Encuentra los mejores productos tecnológicos al mejor precio" 
} = Astro.props
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title}</title>
  <meta name="description" content={description}>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>
<body class="bg-blue-600">
  <!-- Header -->
  <header class="bg-blue-900 shadow-sm sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <!-- Logo -->
        <div class="flex items-center">
          <a href="/" class="text-2xl font-bold text-black">
            🛍️ Tienda
          </a>
        </div>

        <!-- Navegación -->
        <nav class="hidden md:flex items-center space-x-8">
          <a href="/" class="text-white hover:text-black transition-colors">
            Inicio
          </a>
          <a href="/catalogo" class="text-white hover:text-blue-600 transition-colors">
            Catálogo
          </a>
        </nav>

        <!-- Carrito Widget -->
        <div class="flex items-center">
          <CartWidget client:load />
        </div>
      </div>
    </div>
  </header>

  <!-- Contenido principal -->
  <main class="min-h-screen">
    <slot />
  </main>

  <!-- Notificaciones del carrito -->
  <CartNotification client:load />
  
  <!-- Botón de debug del carrito -->
  <CartDebugButton client:load />

  <!-- Footer -->
  <footer class="bg-gray-900 text-white py-12">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        <!-- Información de la tienda -->
        <div class="col-span-1 md:col-span-2">
          <h3 class="text-xl font-bold mb-4">🛍️ Tienda Online</h3>
          <p class="text-gray-400 mb-4">
            Tu tienda de confianza para productos de tecnología y mucho más. 
            Calidad garantizada y envíos rápidos.
          </p>
        </div>

        <!-- Enlaces rápidos -->
        <div>
          <h4 class="font-semibold mb-4">Enlaces</h4>
          <ul class="space-y-2 text-gray-400">
            <li><a href="/" class="hover:text-white transition-colors">Inicio</a></li>
            <li><a href="/catalogo" class="hover:text-white transition-colors">Catálogo</a></li>
            <li><a href="/carrito" class="hover:text-white transition-colors">Carrito</a></li>
          </ul>
        </div>

        <!-- Contacto -->
        <div>
          <h4 class="font-semibold mb-4">Contacto</h4>
          <ul class="space-y-2 text-gray-400">
            <li>📧 info@tienda.com</li>
            <li>📱 +1 234 567 8900</li>
            <li>📍 Ciudad, País</li>
          </ul>
        </div>
      </div>

      <div class="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400">
        <p>&copy; {new Date().getFullYear()} Tienda Online. Todos los derechos reservados.</p>
      </div>
    </div>
  </footer>
</body>
</html>