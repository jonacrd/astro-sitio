---
// Versión simplificada de la página principal sin dependencias de DB
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Town - Compra rápido con IA</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @keyframes fadeInOut {
      0%, 20% { opacity: 1; }
      25%, 95% { opacity: 0; }
      100% { opacity: 1; }
    }
  </style>
</head>
<body class="bg-gray-50">
  <!-- Header -->
  <header class="bg-white shadow-sm border-b">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center py-4">
        <div class="flex items-center">
          <h1 class="text-2xl font-bold text-blue-600">Town</h1>
        </div>
        <nav class="hidden md:flex space-x-8">
          <a href="/" class="text-gray-700 hover:text-blue-600">Inicio</a>
          <a href="/catalogo" class="text-gray-700 hover:text-blue-600">Catálogo</a>
          <a href="/contacto" class="text-gray-700 hover:text-blue-600">Contacto</a>
        </nav>
        <div class="flex items-center space-x-4">
          <button id="btn-login" class="px-4 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700 transition-colors">
            Entrar
          </button>
          <button id="btn-register" class="px-4 py-2 rounded-lg bg-green-600 text-white hover:bg-green-700 transition-colors">
            Registrarse
          </button>
        </div>
      </div>
    </div>
  </header>

  <!-- Hero Section -->
  <section class="min-h-[60vh] flex items-center justify-center bg-gradient-to-br from-blue-900 to-green-700 px-4">
    <div class="max-w-5xl w-full text-center">
      <h1 class="text-4xl md:text-6xl font-bold mb-4 text-white">Town</h1>
      <p class="text-xl text-slate-200 mb-8">Compra rápido, con IA y descuentos locales</p>
      
      <!-- Landbot embed -->
      <div class="bg-white/80 backdrop-blur-sm rounded-2xl shadow-lg p-4 border border-slate-200">
        <script type="module" src="https://cdn.landbot.io/landbot-3/landbot-3.0.0.mjs"></script>
        <div id="myLandbot" style="width: 100%; height: 400px"></div>
        <script type="module">
          var myLandbot = new Landbot.Container({
            container: '#myLandbot',
            configUrl: 'https://storage.googleapis.com/landbot.online/v3/H-3131692-XMRW5AQKBIILVQQU/index.json',
          });
        </script>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">¿Por qué elegir Town?</h2>
        <p class="text-lg text-gray-600">La mejor experiencia de compra con tecnología avanzada</p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <div class="text-center p-6 rounded-lg bg-blue-50">
          <div class="w-16 h-16 bg-blue-600 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-white text-2xl">⚡</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">Compra Rápida</h3>
          <p class="text-gray-600">Proceso de compra optimizado con IA para máxima velocidad</p>
        </div>
        
        <div class="text-center p-6 rounded-lg bg-green-50">
          <div class="w-16 h-16 bg-green-600 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-white text-2xl">🎯</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">Descuentos Locales</h3>
          <p class="text-gray-600">Ofertas exclusivas de vendedores locales verificados</p>
        </div>
        
        <div class="text-center p-6 rounded-lg bg-purple-50">
          <div class="w-16 h-16 bg-purple-600 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="text-white text-2xl">🤖</span>
          </div>
          <h3 class="text-xl font-semibold mb-2">Asistente IA</h3>
          <p class="text-gray-600">Chatbot inteligente para ayudarte en cada paso</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-16 bg-gray-100">
    <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8">
      <h2 class="text-3xl font-bold text-gray-900 mb-4">¿Listo para empezar?</h2>
      <p class="text-lg text-gray-600 mb-8">Únete a Town y descubre una nueva forma de comprar</p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/catalogo" class="px-8 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
          Ver Catálogo
        </a>
        <button id="btn-seller" class="px-8 py-3 bg-amber-600 text-white rounded-lg hover:bg-amber-700 transition-colors">
          Quiero Vender
        </button>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-gray-800 text-white py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h3 class="text-2xl font-bold mb-4">Town</h3>
        <p class="text-gray-400 mb-4">Compra rápido, con IA y descuentos locales</p>
        <div class="flex justify-center space-x-6">
          <a href="/contacto" class="text-gray-400 hover:text-white">Contacto</a>
          <a href="/quienes-somos" class="text-gray-400 hover:text-white">Quiénes Somos</a>
        </div>
      </div>
    </div>
  </footer>

  <script>
    // Funcionalidad básica de autenticación
    const btnLogin = document.getElementById('btn-login');
    const btnRegister = document.getElementById('btn-register');
    const btnSeller = document.getElementById('btn-seller');

    async function refreshAuth() {
      try {
        const response = await fetch('/api/auth/me-universal');
        const { user } = await response.json();
        
        if (user) {
          btnLogin.style.display = 'none';
          btnRegister.style.display = 'none';
          btnSeller.style.display = 'inline-block';
          btnSeller.textContent = 'Mi Cuenta';
          btnSeller.onclick = () => window.location.href = '/dashboard';
        } else {
          btnLogin.style.display = 'inline-block';
          btnRegister.style.display = 'inline-block';
          btnSeller.style.display = 'inline-block';
          btnSeller.textContent = 'Quiero Vender';
        }
      } catch (error) {
        console.log('Usuario no autenticado');
      }
    }

    btnLogin.addEventListener('click', () => {
      alert('Funcionalidad de login en desarrollo');
    });

    btnRegister.addEventListener('click', () => {
      alert('Funcionalidad de registro en desarrollo');
    });

    btnSeller.addEventListener('click', () => {
      alert('Funcionalidad de vendedor en desarrollo');
    });

    // Cargar estado de autenticación
    refreshAuth();
  </script>
</body>
</html>
