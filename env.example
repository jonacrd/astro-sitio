# Variables de entorno necesarias para el proyecto

# Para desarrollo local con SQLite (COPIAR A .env)
DATABASE_URL="file:./prisma/dev.db"

# Base de datos PostgreSQL para producción (Vercel)
# DATABASE_URL="postgresql://username:password@hostname:port/database?sslmode=require"

# Instrucciones para desarrollo local:
# 1. Copiar este archivo a .env: cp env.example .env
# 2. Ejecutar: npx prisma db push
# 3. Ejecutar: npm run seed (opcional, para datos de prueba)
# 4. Ejecutar: npm run dev

# Instrucciones para Vercel:
# 1. Crear una base de datos PostgreSQL (recomendado: Vercel Postgres o Neon)
# 2. Configurar DATABASE_URL en las variables de entorno de Vercel
# 3. El build automáticamente ejecutará "npx prisma db push"




